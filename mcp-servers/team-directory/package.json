{
  "name": "team-directory-mcp-server",
  "version": "1.0.0",
  "description": "Sample MCP server — exposes a team directory (employee lookup, org chart)",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
